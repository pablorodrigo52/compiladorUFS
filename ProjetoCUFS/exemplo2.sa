programa primeiro
inicio
	inteiro : a , b , c ;
	real : r  ,  s  ,  p 
;
	caractere: _DASD_DASDA[10] <- 'string qualquer aqui dentro' // teste // um comentario de linha dentro de um comentario de linha
, g
, h
;
	booleano h <- falso ; 
	se(a=b e nao(c=a) ou r <> s){
		leia(f)     ;
	}
	a <- 1; /* comentando o b 
	b <- 2; */
	c < a + b;
	escreva(c);
fim